<% @photos.each do |photo| %>
  <div>
    <div>
      <%= link_to image_tag(photo.image.url), photo.image.url, target: '_blank' %>
    </div>
    <div>
      <h4><%= photo.title %></h4>
    </div>
  </div>
<% end %>